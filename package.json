{
  "name": "global-group-project",
  "version": "0.0.0",
  // ... autres propriétés
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    // ----------------------------------------------------
    // NOUVELLE COMMANDE POUR LE DÉPLOIEMENT
    // Installez le package 'gh-pages' si ce n'est pas déjà fait : npm install gh-pages --save-dev
    "deploy": "gh-pages -d dist"
    // ----------------------------------------------------
  },
  // ...
}
